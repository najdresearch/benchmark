# Benchmark rules

Every task needs a versioned input/output contract, label guide, scoring code, baseline, held-out split, uncertainty analysis, and stop rule. Never tune on a hidden test set. Report model version, prompt, inference budget, abstention policy, latency, cost, and invalid outputs. Do not commit private cases or employer material.
